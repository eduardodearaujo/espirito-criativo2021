import { styled } from "@stitches/react";

export const Header = styled("header", {
  backgroundImage: "url(/images/bg_preto.jpg)",
  paddingTop: 60,
});

export const HeaderMapa = styled("header", {
  backgroundImage: "#fff",
  padding: "60px 0",
});
